export default function Dummy() {
    return (
        <h1>You are seeing this because 123.tsx file exists in product/ folder and nextJs will not render the specific dynamic route handler file because of it.</h1>
    )   
}